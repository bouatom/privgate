#!/bin/sh
set -e
# Drop leftover console data so a later install is a true first-run.
# Do not run on upgrade — postrm also receives "upgrade" for the old package.
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
  rm -rf /var/lib/privgate
fi
